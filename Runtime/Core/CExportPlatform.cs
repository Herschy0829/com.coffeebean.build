namespace CoffeeBean
{
    /// <summary>导出目标平台。</summary>
    public enum CExportPlatform
    {
        /// <summary>Android（Gradle / Android Studio 工程导出）。</summary>
        Android,

        /// <summary>iOS（Xcode 工程导出）。</summary>
        IOS
    }
}
